---
description: "Use when editing Next.js App Router page and layout TSX files under src/app."
applyTo: "src/app/**/*.tsx"
---

# App Router Page Instructions

- Keep page/layout components focused on composition and orchestration.
- Push reusable business logic into `src/lib` or `src/server` modules.
- Preserve route behavior and URL shape compatibility when refactoring.
